const Oven = ({ ovenDegrees, heatingElement }) => (
  <>
    <p style={{ margin: 0 }}>Heating element is on: {heatingElement}</p>
    <p style={{ margin: 0 }}>Oven degrees: {ovenDegrees}</p>
  </>
);

export default Oven;
