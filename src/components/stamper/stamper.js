import "./stamper.css";

const Stamper = () => (
  <div
    className="stamper"
    style={{
      backgroundColor: "lightgray",
      position: "absolute",
      textAlign: "center",
      height: "50px",
      width: "100px"
    }}
  >
    Stamper
  </div>
);

export default Stamper;
