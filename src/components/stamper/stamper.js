import "./stamper.css";

const Stamper = () => (
  <div
    className="stamper"
    style={{
      backgroundColor: "lightgray",
      textAlign: "center",
      height: "50px",
    }}
  >
    Stamper
  </div>
);

export default Stamper;
